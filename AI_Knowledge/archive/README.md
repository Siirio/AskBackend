# Archive

Use this folder only for historical context that should not drive future implementation by default.

Good candidates:

- old prototype notes;
- old web-staging notes;
- old behavior snapshots;
- migration notes.

Do not archive secrets, auth files, local sqlite state, generated caches, runtime paths, plugin caches, or bulky application code dumps.
